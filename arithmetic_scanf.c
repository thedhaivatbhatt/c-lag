#include <stdio.h>

void main()
{
  int a,b,add,sub,mul,div;
  printf("a value is :");
  scanf("%d",&a);
  printf("b value is :");
  scanf("%d",&b);
  add=a+b;
  sub=a-b;
  mul=a*b;
  div=a/b;

  printf("\n arithmetic the value add %d",add);
  printf("\n arithmetic the value sub %d",sub);
  printf("\n arithmetic the value mul %d",mul);
  printf("\n arithmetic the value div %d",div);


}