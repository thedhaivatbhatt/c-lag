#include <stdio.h>

void main()
{
   float height,ans;

   printf("enter the height");
   scanf("%f",&height);

   if (height<3)
   {
    printf("see the height %f",height);
   }
printf("goodbye..");
}